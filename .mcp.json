{
  "mcpServers": {
    "aikido": {
      "command": "npx",
      "args": ["-y", "@aikidosec/mcp"],
      "env": {
        "AIKIDO_CLIENT_ID": "${AIKIDO_CLIENT_ID}",
        "AIKIDO_API_KEY": "${AIKIDO_API_KEY}"
      }
    }
  }
}
